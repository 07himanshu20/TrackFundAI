[pytest]
# Marker registry only — no testpaths/addopts, so collection behaviour is unchanged.
markers =
    slow: cross-process / large-workbook gates (determinism, full-coverage). Deselect in fast loops with -m "not slow".
