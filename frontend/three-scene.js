/**
 * three-scene.js — stub (Three.js removed for performance)
 * All public methods are no-ops so existing call-sites don't break.
 */
const ThreeScene = (() => {
  return {
    init:                   () => {},
    setCameraTarget:        () => {},
    highlightNodes:         () => {},
    setConstellationOpacity:() => {},
    getCamera:              () => null,
    getScene:               () => null,
  };
})();
